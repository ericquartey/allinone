# Configurazione Server API Proxy
# Copia questo file in .env e modifica i valori se necessario

# Porta server API
PORT=3002

# SQL Server Configuration
DB_USER=sa
DB_PASSWORD=fergrp_2012
DB_SERVER=localhost\\SQL2019
DB_DATABASE=promag
DB_ENCRYPT=false
DB_TRUST_CERT=true

# Ambiente
NODE_ENV=development

# CORS Origins (separati da virgola)
CORS_ORIGINS=http://localhost:3001,http://localhost:3000
