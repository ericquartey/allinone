# EjLog WMS - Production Environment Variables (EXAMPLE)
# Copy this file to .env.production and replace the placeholder values

# API Configuration
# Replace with your actual production API URL
VITE_API_BASE_URL=https://api.your-domain.com/api
VITE_PPC_AUTOMATION_API_BASE_URL=https://automation.your-domain.com
VITE_PPC_AUTOMATION_HUB_BASE_URL=https://automation.your-domain.com
VITE_PPC_BAY_NUMBER=1
VITE_PPC_LANGUAGE=it-IT

# Application Environment
VITE_APP_ENV=production

# Application Version (matches package.json)
VITE_APP_VERSION=2.3.12.4

# WebSocket Configuration
# Set to true if your backend supports WebSocket
VITE_ENABLE_WEBSOCKET=false

# Pagination Settings
VITE_DEFAULT_PAGE_SIZE=20

# Sentry Configuration (Error Tracking)
# Sign up at https://sentry.io and create a React project
# Get your DSN from: Project Settings > Client Keys (DSN)
VITE_ENABLE_SENTRY=true
VITE_SENTRY_DSN=https://examplekey@o123456.ingest.sentry.io/123456
VITE_SENTRY_ENVIRONMENT=production
VITE_SENTRY_TRACES_SAMPLE_RATE=0.1

# Feature Flags
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_DEBUG_TOOLS=false

# Performance Monitoring
VITE_ENABLE_PERFORMANCE_MONITORING=true

# Security Settings
VITE_ENABLE_HTTPS_ONLY=true
VITE_ENABLE_CORS_CREDENTIALS=true

# Build Information (auto-populated during build)
VITE_BUILD_DATE=__BUILD_DATE__
VITE_GIT_COMMIT=__GIT_COMMIT__

# ============================================
# CONFIGURATION INSTRUCTIONS
# ============================================
#
# 1. API_BASE_URL:
#    - Development: http://localhost:8080/api
#    - Staging: https://staging-api.your-domain.com/api
#    - Production: https://api.your-domain.com/api
#
# 2. SENTRY_DSN:
#    - Create account at https://sentry.io
#    - Create new React project
#    - Copy DSN from Project Settings > Client Keys
#
# 3. WEBSOCKET:
#    - Set to false if not using real-time features
#    - Set to true if backend has WebSocket support
#
# 4. TRACES_SAMPLE_RATE:
#    - 1.0 = capture 100% of transactions (expensive)
#    - 0.1 = capture 10% of transactions (recommended)
#    - 0.05 = capture 5% of transactions (high traffic)
#
# ============================================
