# ============================================================================
# EJLOG WMS - Environment Variables Configuration
# ============================================================================

# Database Configuration
DB_SERVER=localhost\\SQL2019
DB_DATABASE=promag
DB_USER=sa
DB_PASSWORD=your_password_here
DB_ENCRYPT=false
DB_TRUST_SERVER_CERTIFICATE=true

# Server Configuration
PORT=7077
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-secret-key-here-change-in-production
JWT_EXPIRATION=24h

# AI Assistant Configuration
# ============================================================================

# Abilita/Disabilita AI Assistant
AI_ENABLED=true

# Modello AI predefinito: 'claude' | 'gpt4' | 'local'
AI_DEFAULT_MODEL=claude

# Anthropic Claude API
# Ottieni la tua API key su: https://console.anthropic.com/
# Formato: sk-ant-api03-...
# IMPORTANTE: Inserire manualmente la propria API key qui sotto
ANTHROPIC_API_KEY=

# OpenAI GPT-4 API (opzionale - alternativa a Claude)
# Ottieni la tua API key su: https://platform.openai.com/api-keys
# Formato: sk-...
# OPENAI_API_KEY=

# AI Configuration
AI_MAX_TOKENS=1024
AI_TEMPERATURE=0.7
AI_CONTEXT_DEPTH=standard

# ============================================================================
# Note:
#
# 1. Copia questo file in `.env` nella root del progetto
# 2. Sostituisci i placeholder con i tuoi valori reali
# 3. NON committare il file `.env` su Git (è in .gitignore)
# 4. Per l'AI Assistant è richiesta ALMENO una API key (Claude o OpenAI)
# 5. Se entrambe sono configurate, userà AI_DEFAULT_MODEL
# 6. Se nessuna è configurata, il sistema userà mock responses
#
# ============================================================================
