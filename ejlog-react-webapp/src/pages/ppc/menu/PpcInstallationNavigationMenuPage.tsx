import React from 'react';
import PpcInstallationNavMenu from '../../../components/ppc/PpcInstallationNavMenu';

const PpcInstallationNavigationMenuPage: React.FC = () => {
  return (
    <div className="ppc-installation-only">
      <PpcInstallationNavMenu />
    </div>
  );
};

export default PpcInstallationNavigationMenuPage;
